Термины:

    Экспортер:
        
        Это демон/контейнер который открывает порт на прослушивание для prometheus.
        
        Т.е prometheus с заданной периодичностью (в prometheus.yml) ходит в этот 
        порт и забирает метрики.
